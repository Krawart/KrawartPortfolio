import {makeStyles} from '@material-ui/core';

export const useStackPageStyles = makeStyles({
    root: {
        display: 'flex',
        flex: 1,
        alignItems: 'center',
        height: '100%',
    },
    text: {},
    infographic: {
        marginBottom: '4rem'
    },
});

